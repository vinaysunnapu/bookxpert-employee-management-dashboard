/**
 * Form validation error messages
 * Maps field names to error strings
 */
export interface FormErrors {
  fullName?: string;
  dateOfBirth?: string;
  state?: string;
  gender?: string;
  profileImage?: string;
}
